const toneClass = { forest: 'bg-forest', terracotta: 'bg-terracotta', moss: 'bg-moss', other: 'bg-other' }
export default function StatCard({ icon: Icon, label, value, trend, tone = 'forest' }) { return <article className="panel p-5"><div className="flex items-start justify-between"><p className="text-sm font-medium text-moss">{label}</p><span className={`grid h-9 w-9 place-items-center ${toneClass[tone] || toneClass.forest} text-paper`}><Icon size={18}/></span></div><p className="mt-4 font-display text-3xl text-forest">{value}</p>{trend && <p className="mt-2 text-xs text-moss">{trend}</p>}</article> }
