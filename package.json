{
  "name": "smartseg",
  "private": true,
  "version": "0.1.0",
  "description": "SmartSeg monorepo workspace root",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "frontend:dev": "npm --workspace frontend run dev",
    "frontend:build": "npm --workspace frontend run build"
  }
}
